#include "../header/AcceptInput.h"

Vector2 AcceptInput::GetLClickWorldPos()
{
	// マウスの位置を取得
	// スクリーン座標をワールド座標に変換
	return Vector2().zero();
}

Vector2 AcceptInput::GetRClickWorldPos()
{
	// 入力されているか確認

	// 入力座標をワールド座標に変換
	return Vector2().zero();
}